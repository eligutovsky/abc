# abc

An CLI utility for printing English alphabet in a terminal

## usage

```bash
git clone git@github.com:eligutovsky/abc.git
cd abc
cp abc.sh /usr/local/bin/abc
```

> [!NOTE]
> To solve `ln: /usr/local/bin/abc: Permission denied` error -- use `sudo` command
